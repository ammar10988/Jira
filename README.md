## 🏗️ Built With
- **Python 3.x**
- **Django 5.x**
- **SQLite** (development)
- **HTML / CSS / JavaScript**
- **Bootstrap (custom UI styling)**
- **Django Authentication (Boss login)**
- **Custom OTP Login (Team Leads & Employees)**



sudo apt update && sudo apt upgrade -y
sudo apt install python3 python3-pip python3-venv -y
sudo apt install nginx -y
sudo apt install sqlite3 -y    # If using SQLite (your project is)
sudo apt install git -y
python-dotenv
django-cleanup
